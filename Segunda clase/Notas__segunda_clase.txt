// arreglos
fun arreglosKotlin(){
    val arrayNumeros: IntArray = intArrayOf(12,13,3,4,21)
	val paises = ArrayList<String>()
    paises.add("Peru")
    paises.add("Brasil")
}

// val son consideradas constantes
// var son consideradas variables
fun bucleFor(){
    val notas = intArrayOf(13, 20, 4, 6, 8)
    for(nota: Int in notas){
        println/(nota.toString())
    } // toma dato por valor e indice
    for((indice, valor) in notas.withIndex()){
        println("Nota $indice es igual $valor")
    } // se puede recorrer intervalos de nros
    for(numero in 5 .. 15){
        println("Numero $numero")
    }
}

// bucles como el doWhile
fun doWhile(){
    var contador = 15
    while (contador > 1) {
        
    } do{
       // deja pasar y luego consulta 
    }while (contador > 1)    
}

// ejemplos de funciones
fun ejemplosLambda() {
    val alumno = arrayListOf(9. 10. 30. 40. 1)
    n -> if(n % 2 == 0){
        return true
    } else {
        return false
    }
}

// parte 2da clase
// funciones de multiplicacion
val miFuncionMultiplica = fun (n1: Int, n2:Int): Int{
    return n1 * n2
}

operaciones(10, 4 , miFuncionMultiplica)
operaciones(12, 8, {x, y -> x +y})

// funciones de lambda en una sola linea
private fun operaciones(x: Int, Z: Int, operacionesLambda:(Int,Int) -> Int):Int{
    return operacionLambda(x, z)
}


// ----------------
// herencia 
class Animal {
fun emitirSonido() {
}
fun trasladarse(){
}

// lo llamas como
interface IAnimal {
fun emitirSonido()
fun trasladarse()
fun cazar()
}


// parte de herencia
class Animal : IAnimal {
	oveeride fun emitirSonido() {
		TODO( "Not yet implemented")
	}
	oveeride fun trasladarse() {
		TODO( "Not yet implemented")
	}
	oveeride fun cazar() {
		TODO( "Not yet implemented")
	}
}


// para qye ka gerebcua aokuqye sobre la clase hija
class Pulpo : Animal() {
	override fun emiturSonido(){
		super.emitirSonido()
	}




// parte de los grupos 23:57 30/4/2026
50% -- > 1:30:50 

// Data class
// sirven para crear clases con sus respectivos atributos

1:40 me quede

